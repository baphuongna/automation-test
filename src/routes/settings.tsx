export default function Settings() {
  return (
    <section className="route-skeleton" data-testid="route-settings">
      <span className="route-skeleton__eyebrow">Settings bootstrap</span>
      <h1>Settings</h1>
      <p>Placeholder screen for baseline application configuration and startup preferences.</p>
    </section>
  );
}
