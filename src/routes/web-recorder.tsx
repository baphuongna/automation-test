export default function WebRecorder() {
  return (
    <section className="route-skeleton" data-testid="route-web-recorder">
      <span className="route-skeleton__eyebrow">Route skeleton</span>
      <h1>Web Recorder</h1>
      <p>Placeholder screen for recording browser interactions in a separate window.</p>
    </section>
  );
}
