export default function DataManager() {
  return (
    <section className="route-skeleton" data-testid="route-data-manager">
      <span className="route-skeleton__eyebrow">Route skeleton</span>
      <h1>Data Manager</h1>
      <p>Placeholder screen for maintaining reusable test data tables.</p>
    </section>
  );
}
