Task: T5 Secret storage baseline rerun
Date: 2026-03-30

Scope verified in source:
- `src-tauri/src/models/environment_variable.rs` now enforces `validate_for_storage()` so secret variables cannot be persisted unless the value looks encrypted and `masked_preview` is present.
- `src-tauri/src/repositories/environment_repository.rs` calls `validate_for_storage()` before insert/update of environment variables, blocking plaintext secret persistence through the repository layer.
- `src-tauri/src/services/secret_service.rs` continues to encrypt/decrypt via AES-256-GCM and only exposes masked previews for secret display paths.
- `src-tauri/src/contracts/dto.rs` still exposes `value_masked_preview` rather than raw secret values.

Plaintext leakage review:
- Grep review found no production persistence/log path that writes known plaintext secret literals.
- Matches for plaintext-like strings were confined to Rust unit tests in `secret_service.rs`, `environment_variable.rs`, and `environment_repository.rs`.

Verification command limitations in this environment:
- `cargo test` could not run: `cargo` is not installed.
- `cargo build` could not run: `cargo` is not installed.
- Rust LSP diagnostics could not run: `rust-analyzer` is not installed.

Result:
- Source-level protections for encrypted-at-rest persistence and masked preview semantics were repaired and revalidated.
- Runtime/build verification remains blocked by missing Rust toolchain components in the provided environment.
